AUDIT CONFIG & SECRETS — SurfLab v3 — mer. 22 juil. 2026 10:58:37 UTC
======================================================================

### 1. Pare-feu (explique les ports filtrés) ###
-- ufw status --
(ufw présent, statut nécessite root)
-- iptables (compte de règles par chaîne, sans root si possible) --
-- nftables --

### 2. Exposition du socket Docker aux conteneurs (risque d'évasion) ###
cs2-manager-api :
     SOCKET /var/run/docker.sock->/var/run/docker.sock(RW)
monitoring-api-1 :
     SOCKET /var/run/docker.sock->/var/run/docker.sock(RO)
monitoring-caddy-1 :
monitoring-frontend-1 :
cs2-surf-27027 :

### 3. Permissions des fichiers de secrets (.env, credentials) ###
-rw------- surflab:surflab /home/surflab-v3/backend/.env
-rw------- surflab:surflab /home/surflab-v3/frontend/.env
absent: /home/surflab-v3/monitoring/frontend/.env
-rw------- surflab:surflab /home/surflab/.git-credentials
-rw------- surflab:surflab /home/surflab/.claude/.credentials.json

### 4. Secrets potentiellement suivis par git ###
-- .env dans l'index git ? --
   aucun fichier sensible suivi (bon)
-- .gitignore couvre .env ? --
# Environnement et Secrets (CRITIQUE)
.env
.env.local
.env.*
!.env.example
